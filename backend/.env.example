# ============================================================
#  Tap DAO — Environment Variables Template
#  Copy this file to .env and fill in your real values.
#  NEVER commit .env — it is in .gitignore.
# ============================================================

# ── AI / LLM (Google Colab — free) ──────────────────────────
# Run colab/tap_dao_ai_server.py in Google Colab (Runtime → T4 GPU).
# The notebook will print a public URL. Paste it below.
COLAB_AI_URL=https://xxxx.ngrok-free.app
